#include <bits/stdc++.h>
using namespace std;

int main () {
    double e, f, g, x, y, z;
    cin >> e >> f >> g >> x >> y >> z;
    cout << fixed << setprecision(2) << "Torno amb " << ((e*x - f)*y - g)*z << " euros." << endl;
}
